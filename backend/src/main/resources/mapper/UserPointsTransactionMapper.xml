<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.UserPointsTransactionMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.entity.UserPointsTransaction">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="type" property="type" />
        <result column="amount" property="amount" />
        <result column="balance_after" property="balanceAfter" />
        <result column="description" property="description" />
        <result column="reference_id" property="referenceId" />
        <result column="idempotency_key" property="idempotencyKey" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, user_id, type, amount, balance_after, description, reference_id, idempotency_key, created_at
    </sql>

    <!-- 插入交易记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_points_transaction (
            user_id, type, amount, balance_after, description, reference_id, idempotency_key, created_at
        ) VALUES (
            #{userId}, #{type}, #{amount}, #{balanceAfter}, #{description}, #{referenceId}, #{idempotencyKey}, NOW()
        )
    </insert>

    <!-- 根据用户ID查询交易记录 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_points_transaction
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户ID分页查询 -->
    <select id="selectByUserIdWithPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_points_transaction
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>