<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.UserCheckinMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.entity.UserCheckin">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="checkin_date" property="checkinDate" />
        <result column="continuous_days" property="continuousDays" />
        <result column="points_earned" property="pointsEarned" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, user_id, checkin_date, continuous_days, points_earned, created_at
    </sql>

    <!-- 插入签到记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_checkin (
            user_id, checkin_date, continuous_days, points_earned, created_at
        ) VALUES (
            #{userId}, #{checkinDate}, #{continuousDays}, #{pointsEarned}, NOW()
        )
    </insert>

    <!-- 根据用户ID和日期查询 -->
    <select id="selectByUserIdAndDate" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_checkin
        WHERE user_id = #{userId} AND checkin_date = #{checkinDate}
    </select>

    <!-- 查询用户最近的签到记录 -->
    <select id="selectLatestByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_checkin
        WHERE user_id = #{userId}
        ORDER BY checkin_date DESC
        LIMIT 1
    </select>

    <!-- 查询用户连续签到天数 -->
    <select id="getContinuousDays" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(continuous_days), 0)
        FROM user_checkin
        WHERE user_id = #{userId}
    </select>

    <!-- 根据用户ID查询签到记录 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_checkin
        WHERE user_id = #{userId}
        ORDER BY checkin_date DESC
    </select>

</mapper>